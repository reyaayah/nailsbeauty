import CollectionPage from "@/components/CollectionPage";

export default function BundlesPage() {
    return (
        <CollectionPage
            collection="bundles"
            filters={{}}
            showFilters={false}
        />
    );
}